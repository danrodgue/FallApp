package com.example.fallapp.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta principal FallApp (vibrant-warm)
// Fondo general
val CreamBackground = Color(0xFFFFF1EC) // #FFF1EC

// Superficies / campos
val PeachSurface = Color(0xFFFFD1C3) // #FFD1C3

// Bordes / acentos
val RedAccent = Color(0xFFFF5757) // #FF5757

// Botones de acción principales
val OrangeAction = Color(0xFFFF914D) // #FF914D

// Texto e iconos
val DarkText = Color(0xFF3A3230) // #3A3230

// Variantes derivadas útiles
val DarkTextSecondary = Color(0x993A3230) // mismo tono, más transparente
val DisabledSurface = Color(0xFFFFE2D7)
val ErrorColor = RedAccent
