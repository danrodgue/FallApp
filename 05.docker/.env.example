# =============================================================================
# Configuración de FallApp - Archivo .env
# =============================================================================
# Copiar este archivo a .env y personalizar con tus valores
#
# Para desarrollo local:
#   cp .env.example .env
#
# Para producción:
#   Usar variables de entorno del servidor o gestor de secretos
# =============================================================================

# =============================================================================
# PostgreSQL
# =============================================================================
POSTGRES_DB=fallapp
POSTGRES_USER=fallapp_user
POSTGRES_PASSWORD=fallapp_secure_password_2026
POSTGRES_PORT=5432
POSTGRES_DATA_PATH=./postgres_data

# =============================================================================
# Spring Boot / API
# =============================================================================
API_USER=admin
API_PASSWORD=admin1234
API_PORT=8080

# JPA/Hibernate - Opciones para ddl-auto:
# - create: Crea tablas nuevas (ELIMINA datos existentes)
# - create-drop: Crea y borra al detener
# - update: Actualiza esquema (seguro)
# - validate: Solo valida (recomendado para producción)
SPRING_JPA_DDL_AUTO=validate

# =============================================================================
# pgAdmin (Herramienta de administración - SOLO DESARROLLO)
# =============================================================================
PGADMIN_EMAIL=admin@fallapp.local
PGADMIN_PASSWORD=admin1234
PGADMIN_PORT=5050

# =============================================================================
# Aplicación
# =============================================================================
ENV=development
# Opciones: development, staging, production

# =============================================================================
# Hugging Face - Análisis de sentimiento (red neuronal)
# =============================================================================
# Token para analizar sentimiento de comentarios (positive/neutral/negative).
# Obtener en: https://huggingface.co/settings/tokens
# Spring Boot mapea: HUGGINGFACE_API_TOKEN -> huggingface.api.token
HUGGINGFACE_API_TOKEN=